# Databricks notebook source
# MAGIC %md
# MAGIC **Records are processed from many locations in diff formats, has to be cleaned, validated and aggregated for business reporting, use dlt(delta live tables) pipleine, the raw data ingested from cloud storage, transformed with quality checks and stored in delta tables for analytics dashboard ensuring real time reliaility and accuracy, its in first.py**

# COMMAND ----------

